# PyTorch and torchvision are preinstalled and intentionally omitted so pip cannot replace the CUDA build.
# Prefer: bash scripts/setup_env.sh
segmentation-models-pytorch>=0.5,<0.6
rasterio>=1.4,<2
wandb>=0.28,<0.29
tqdm>=4.67,<5
accelerate>=1.10,<2
transformers==4.50.0
safetensors>=0.8,<0.9
timm==1.0.15
einops==0.8.1
ninja>=1.13,<2
mamba-ssm==2.3.2.post1
pytest>=8.4,<9
